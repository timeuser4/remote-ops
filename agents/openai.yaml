interface:
  display_name: "Remote Tmux"
  short_description: "SSH + tmux remote terminal multiplexer with persistent sessions"
  default_prompt: "Use $rtmux to execute commands on remote hosts via persistent tmux sessions. All commands run inside tmux, preserving terminal history and environment context."
